# Este archivo es un ejemplo de configuración
# Copia este archivo como .env en cada servicio y ajusta los valores según tu entorno

# Para desarrollo local con Docker, los valores por defecto funcionan correctamente
# Solo necesitas copiar los archivos .env.example de cada servicio como .env

# Servicios que requieren .env:
# - orders-producer-python/.env
# - orders-producer-node/.env
# - api-gateway/.env
# - admin-service/.env
